# Master Context Document — Baseline

See the dated snapshot in `context/context-2026-07-13.md` (also linked from `context/context-latest.md`).

Future architectural updates may add `mcd_YYYY-MM-DD.md` files here per the process-task-list prompt; prefer keeping the live brief in `context/` and using this folder for longer-form MCD copies when needed.
